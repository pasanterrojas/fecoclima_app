<script setup lang="ts">definePageMeta({layout:'admin',middleware:'admin-auth'}); const fields=[{key:'station_id',label:'ID estación',required:true},{key:'crop_id',label:'ID cultivo',required:true},{key:'date_from',label:'Desde',type:'date',required:true},{key:'date_to',label:'Hasta',type:'date',required:true}] as const</script>
<template><AdminCrudPage title="Reportes" description="Cola, archivos generados y errores de procesamiento." endpoint="/reports" :fields="fields as any" :columns="[{key:'title',label:'Título'},{key:'station_name',label:'Estación'},{key:'crop_name',label:'Cultivo'},{key:'status',label:'Estado'},{key:'created_at',label:'Creado'},{key:'completed_at',label:'Completado'}]"/></template>
